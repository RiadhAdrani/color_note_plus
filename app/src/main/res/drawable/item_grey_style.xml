<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/grey_spanish">

    <!-- on hold click effect
        better be different from the on click effect -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/grey_darker_davys" />
            <corners android:radius="10dp" />
        </shape>
    </item>

    <!-- Default style -->
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <gradient
                android:angle="45"
                android:endColor="@color/grey_spanish"
                android:startColor="@color/grey_darker_davys"
                android:type="linear" />
            <corners android:radius="10dp" />
        </shape>
    </item>

</ripple>